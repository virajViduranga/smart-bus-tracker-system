import React from 'react';

/*
  Input Component
  ---------------
  A reusable input field with:
  ✔ Optional left-side icon
  ✔ Tailwind styling
  ✔ Spread props to allow any input attributes (placeholder, type, etc.)

  Props:
    icon → React icon component from lucide-react or similar
    ...props → Any additional HTML input properties
*/

const Input = ({ icon: Icon, ...props }) => (
  <div className="relative">
    
    {/* Icon container (only shown if icon is provided) */}
    <div className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400">
      {Icon && <Icon size={20} />}  
      {/* If Icon exists → render the icon with size 20 */}
    </div>

    {/* Input field */}
    <input
      className="
        w-full
        pl-12        /* Padding left for icon space */
        pr-4         /* Right padding */
        py-4         /* Vertical padding */
        rounded-2xl  /* Smooth round edges */
        border border-gray-200
        bg-gray-50
        text-gray-800
        placeholder-gray-400
        focus:outline-none
        focus:ring-2 focus:ring-blue-500/50
        focus:border-blue-500
        transition-all
      "
      {...props}   // Pass all other props (e.g., placeholder, type, onChange)
    />
  </div>
);

export default Input;
